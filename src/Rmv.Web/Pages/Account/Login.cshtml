@page
@model Rmv.Web.Pages.Account.LoginModel
@* Redirect-only. OnGet always returns a Challenge or a Redirect, so this
   template is never rendered. It exists because Razor Pages routes on the
   .cshtml file, not the model. *@
