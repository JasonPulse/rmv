@page "/tools/signature/background"
@model Rmv.Web.Pages.Tools.SignatureBackgroundModel
@* No UI. This page exists so the upload can carry a rate limit attribute, which
   Razor Pages ignores on a handler method; see SignatureBackgroundModel. A GET
   redirects to the editor. *@
